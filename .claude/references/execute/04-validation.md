# /execute — Step 4: Validation

After all tasks are complete:

1. Run the project's full test suite.
2. Run lint/type-check if available.
3. Verify the application starts without errors.

This is a quick sanity check — full verification (visual, security, code review, auto-evolve) lives in `/validate`.
