# v0.5.1 — README sync to v0.5 framework state

## Changed

- **README** brought into sync with the v0.5.0 framework state:
  - Added the **Production Code vs. Design Artifacts** table and the 3-question clarifying script reference.
  - Documented the **`huashu-design`** + **`/brand-extract`** pairing, project-state branching (fresh / token-driven / ad-hoc), and the per-repo license-acknowledgement gate.
  - Expanded the **Review Layers** table from two layers to three: Superpowers Code Review (`/validate` Phase 5), 5D Visual Critique (`/validate` Phase 2.5, design-artifact only), and Codex Adversarial Review (`/ship`, optional).
  - Updated the **What's Inside** tree to current counts: 15 commands, 4 agents, 3 framework-local skills, 9 rules + template, 8 hooks, plus `.versions.json`.
  - Surfaced the spec-reviewer marker enforcement hooks alongside branch protection and reminders.
  - Linked the changelog directory.
- **README review pass** caught and fixed:
  - **CLI Reference** now lists `lean-index` and `merge-settings` subcommands (previously omitted; both are exposed by `cli/index.js` and shown in `--help`).
  - **KB structure diagram** now shows `_search/lean-index.json` alongside `_search/index.json` (the framework writes both atomically).
  - **Pipeline Commands** table now includes `/merge-configs` for post-init/update reconciliation.
- **`package.json`** version bumped to `0.5.1`.

## Migration

No code or config changes. Existing installs can update purely for the docs:

```bash
npx ai-development-framework update
```
